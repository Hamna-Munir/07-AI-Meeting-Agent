# Groq API (OpenAI-compatible)
OPENAI_API_KEY=your_groq_api_key_here

# App Settings
APP_ENV=development
LOG_LEVEL=INFO

# Note: Google Calendar credentials are NOT set here.
# They live in credentials.json (downloaded from Google Cloud Console)
# and token.json (generated on first OAuth login) — both gitignored.
